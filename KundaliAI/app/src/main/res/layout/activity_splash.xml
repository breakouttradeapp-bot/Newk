<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_splash_gradient">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Zodiac Ring -->
        <ImageView
            android:id="@+id/ivZodiacRing"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/ic_zodiac_ring"
            android:contentDescription="@string/zodiac_wheel" />

        <Space android:layout_height="24dp" android:layout_width="wrap_content"/>

        <!-- App Name -->
        <TextView
            android:id="@+id/tvAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textSize="36sp"
            android:textStyle="bold"
            android:textColor="#FFD700"
            android:letterSpacing="0.05"
            android:alpha="0" />

        <Space android:layout_height="12dp" android:layout_width="wrap_content"/>

        <!-- Tagline -->
        <TextView
            android:id="@+id/tvTagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tagline"
            android:textSize="14sp"
            android:textColor="#B0BEC5"
            android:textStyle="italic"
            android:letterSpacing="0.08"
            android:alpha="0" />

    </LinearLayout>

    <!-- Version at bottom -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="32dp"
        android:text="@string/version"
        android:textSize="11sp"
        android:textColor="#546E7A" />

</FrameLayout>
